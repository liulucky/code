模型(Model) 用于封装与应用程序的业务逻辑相关的数据以及对数据的处理方法

视图(View) 提供给用户的操作界面。用来显示数据。在View中一般程序上的逻辑。

控制器(Controller) 起到不同层面间(模型、视图)组织的作用，用于控制应用程序的流程。


对MVC的理解：

比如一家商场，完全可以分成三部分。一部分是仓库，负责提供商品，这是"功能层"（或者"模型层"）；

另一部分是零售铺面，负责销售商品，这是它的"视图层"；

两者之间就是"控制层"，包括柜台和仓库之间一切互动的机制。